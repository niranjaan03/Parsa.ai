"""
Adapters Init — Pluggable extraction engine adapters.

Each adapter wraps a specific OCR engine behind a common interface
so engines can be swapped, combined, or A/B tested without touching
downstream pipeline stages.
"""
